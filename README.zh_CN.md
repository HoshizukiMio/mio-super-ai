# ⚡ Mio's SuperAI (Mio 的人工智障)

[English](README.md) | [**简体中文**](README.zh_CN.md)

[试一试](https://super-ai.hoshizukimio.com/)

> "大概是你用过的最离谱的 AI API。"

**Mio's SuperAI** 是一个基于 Cloudflare Workers 的伪 OpenAI API 提供商。它虽然拥有和 OpenAI 一样的接口格式，但只会返回基于配置文件设定的虚假内容、随机的报错以及充满二次元梗的回复。

它包含一个“心情系统”（随机报错）、多种搞怪的人格模型，以及一个精美的 Web 介绍页面。非常适合用来测试你的应用容错能力，或者单纯用来整蛊朋友。

## ✨ 特性

- **OpenAI 格式兼容**: 支持 `/v1/chat/completions`, `/v1/images/generations`, `/v1/audio/speech` 等全套接口。
- **完全可配置的模型**: 在 `src/config.js` 中定义你自己的模型（如 `mio-flash-v1`, `mio-otaku-xl`）。
- **混沌模式**: 可配置 `errorRate`（例如 20%），随机返回 500 错误（如“Mio 正在吃布丁没空理你”）。
- **关键词触发**: 设定特定关键词的固定回复（如输入“原神” -> 回复“启动！”）。
- **特殊逻辑模式**: 支持“逻辑大师”模式（去除问号、把疑问句变陈述句）等。
- **严格校验**: 拒绝 `gpt-4` 等常规模型名，强制用户使用你定义的 Mio 模型。
- **精美界面**: 内置基于 TailwindCSS 的主页，展示 API Key 和模型列表。

## 📂 项目结构

```text
mio-super-ai/
├── wrangler.toml        # Cloudflare 配置文件
└── src/
    ├── config.js        # 核心配置（模型、报错语料、关键词）
    ├── utils.js         # 工具函数与正则处理
    ├── templates.js     # HTML 页面模板
    ├── api.js           # API 业务逻辑 (聊天、绘图、语音...)
    └── index.js         # 路由分发
```

## 🚀 部署指南

### 准备工作
- [Node.js](https://nodejs.org/)
- [Wrangler](https://developers.cloudflare.com/workers/wrangler/install-and-update/) (`npm install -g wrangler`)

### 步骤

1. **下载代码**：保存所有文件到项目目录。
2. **安装依赖**：
   ```bash
   npm install
   ```
3. **本地测试**：
   ```bash
   wrangler dev
   ```
   打开浏览器访问 `http://localhost:8787` 查看效果。

4. **发布到 Cloudflare**：
   ```bash
   wrangler deploy
   ```

## ⚙️ 配置说明

所有好玩的内容都在 `src/config.js` 中。

```javascript
export default {
  apiKey: "sk-mio-is-watching-you", // 你的 API Key
  errorRate: 0.2, // 20% 概率报错
  
  // 自定义报错信息
  errorMessages: [
    "服务器被猫咬断了网线。",
    "Mio 拒绝回答并向你扔了一个 Bug。"
  ],

  // 定义模型
  models: {
    "mio-otaku-xl": {
      type: "chat",
      description: "二次元浓度 1000%。",
      keywords: { "JOJO": "欧拉欧拉！" },
      responses: ["错的不是我，是世界。", "爆裂吧现实！"]
    }
  }
};
```

## 🔌 API 调用

**Base URL**: `https://[你的 Worker 域名]/v1`
**API Key**: 查看 `src/config.js` (默认: `sk-mio-is-watching-you-super-secret-key`)

### 1. 聊天 (Chat Completion)
**注意：** 你必须使用 `config.js` 中定义的模型 ID。如果请求 `gpt-3.5` 会直接返回 404 错误。

```bash
curl https://[your-domain]/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-mio..." \
  -d '{
    "model": "mio-otaku-xl",
    "messages": [{"role": "user", "content": "JOJO"}]
  }'
```

### 2. 绘图 (Fake DALL-E)
返回配置好的随机图片（如 HTTP 猫）。

```bash
curl https://[your-domain]/v1/images/generations \
  -H "Content-Type: application/json" \
  -d '{ "model": "mio-paint-v1", "prompt": "画一只猫" }'
```

## ⚠️ 免责声明

本项目仅供娱乐和学习使用。它不提供任何真实的人工智能服务。请勿在需要高可靠性的生产环境中使用（除非你想故意折磨你的用户）。
